import type { Metadata } from "next";
import { Wallpoet, Poppins, Prosto_One, Press_Start_2P, Aoboshi_One } from "next/font/google";
import "./globals.css";

const wallpoet = Wallpoet({
  variable: "--font-wallpoet",
  subsets: ["latin"],
  weight: "400",
});

const poppins = Poppins({
  variable: "--font-poppins",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
});

const prostoOne = Prosto_One({
  variable: "--font-prosto-one",
  subsets: ["latin"],
  weight: "400",
});

const pressStart = Press_Start_2P({
  variable: "--font-pressStart",
  subsets: ["latin"],
  weight: "400",
});

const aoboshi = Aoboshi_One({
  variable: "--font-aoboshi",
  subsets: ["latin"],
  weight: "400",
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${poppins.variable} ${wallpoet.variable} ${prostoOne.variable} ${pressStart.variable} ${aoboshi.variable} --font-poppins antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
